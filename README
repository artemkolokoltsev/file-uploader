# File uploading dashboard

Clone the project and use the following commands to run it locally:

### **Frontend (Vite)**

```bash
cd frontend
npm install
npm run dev
```

---

### **Backend (.NET)**

```bash
cd Backend/Backend
dotnet build
dotnet run
```
---


* Backend running on: `http://localhost:5045`
* Frontend running on: `http://localhost:5173`